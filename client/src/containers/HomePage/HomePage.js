import React from "react";
import styled from "styled-components";
import PageContainer from "../../components/UI/PageContainer";

const HomePage = () => {
  return <PageContainer>This is the Home Page</PageContainer>;
};

export default HomePage;
